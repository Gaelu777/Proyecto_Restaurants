services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
    - .:/app
    ports:
    - "8000:8000"
    environment:
    - DEBUG=1
    depends_on:
    - db
  db:
    image: mariadb:10.11
    environment:
      MARIADB_DATABASE: restaurants
      MARIADB_ROOT_PASSWORD: ''
      MARIADB_ALLOW_EMPTY_ROOT_PASSWORD: 'yes'
    ports:
      - "3307:3306" # Exponemos el puerto 3307 en el host para evitar conflictos con MariaDB local
    volumes:
      - db_data:/var/lib/mysql
volumes:
  db_data: